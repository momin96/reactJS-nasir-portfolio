# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
      - uses: firebase/actions/cli@master
        with:
          node-version: 16
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ITSNASIRTECH }}'
          channelId: live
          projectId: itsnasirtech
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
          CI: false
      
  
  #deploy:
    #runs-on: ubuntu-latest
    
    #steps:
    #- name: Checkout code
      #uses: actions/checkout@v2
      
    #- name: Set up Node.js
      #uses: actions/setup-node@v2
      #with:
        #node-version: 14
        
    #- name: Install dependencies
      #run: npm ci

    #- name: Build the app
      #run: npm run build
      #env:
        #CI: false
      
    #- name: Install Firebase CLI
      #run: npm install -g firebase-tools
      
    #- name: Login to Firebase
      #run: firebase login --token ${{ secrets.GITHUB_TOKEN }}

    #- name: Deploy to Firebase
      #run: firebase deploy --only hosting --token ${{ secrets.GITHUB_TOKEN }}
    
 
